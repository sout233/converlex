use std::fs;
use std::path::Path;

fn main() {
    // let variants = vec!["Mp3", "Flac", "Wav", "Aac"];

    // let mut output = String::new();

    // for variant in variants {
    //     let ext = variant.to_lowercase();
    //     output.push_str(&format!("        Audio::{} => \"{}\",\n", variant, ext));
    // }

    // let out_dir = std::env::var("OUT_DIR").unwrap();
    // let dest_path = Path::new(&out_dir).join("audio_ext_match.rs");
    // fs::write(dest_path, output).unwrap();
}
